/**
 * Prism Design System — public entrypoint.
 *
 * Import visual values from here (or from a semantic theme via `useTheme()`),
 * never from a component-local constant.
 */

export { palette, ansiFallback, spacing, rhythm, motion, border } from './tokens'
export type { Palette, Spacing } from './tokens'

export { prismThemes } from './themes'
